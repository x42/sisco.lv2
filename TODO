* allow pre-delay after trigger before starting acquisition
* mouse interaction w/canvas (drag cursor, ...)
* vertical markers, cursor
* numeric readout of level-delta (of x and y cursor)
* X/Y mode (-> goniometer), bitscope
* memory, store/recall data
* color picker and Grid options
* runtime resizable display (unhardcode DAWIDTH/DAHEIGHT)
* multi-channel variant (break out DAHEIGHT * channel scaling)
* re-layout - esp text, marker and trigger state
* external trigger input (audio, midi, control)
* rewrite using instance-access and semaphore to notify UI

- switch to robtk (openGL + gtk) -- completed pending robtk submodule
* robtk: dial acceleration, range
* robtk: select colors
